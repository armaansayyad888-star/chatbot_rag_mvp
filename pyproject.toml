[project]
name = "chatbot-rag-mvp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "flask>=3.1",
  "boto3>=1.35",
  "requests>=2.32",
  "requests-aws4auth>=1.3",
  "python-dotenv>=1.0",
]
